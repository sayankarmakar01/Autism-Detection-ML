<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Main app theme -->
    <style name="Theme.Cognisite" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Splash screen theme for all API levels -->
    <style name="Theme.Cognisite.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Static background during splash init -->
    <item name="android:windowBackground">@drawable/splash_screen</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowFullscreen">true</item>
</style>
</resources>
