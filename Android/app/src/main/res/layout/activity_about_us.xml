<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/background" /> <!-- Replace with your image -->

    <!-- Dark Overlay -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80000000" /> <!-- Semi-transparent black -->

    <!-- Foreground Scrollable White Card -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/foreground_card_bg"
            android:padding="32dp"
            android:elevation="6dp"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="24dp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:gravity="center_horizontal">

            <!-- Logo -->
            <ImageView
                android:id="@+id/logo"
                android:layout_width="2000dp"
                android:layout_height="180dp"
                android:layout_marginBottom="16dp"
                android:contentDescription="App Logo"
                android:src="@drawable/logo" /> <!-- Replace with your logo -->

            <!-- Title -->
            <TextView
                android:id="@+id/about_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:text="About Us"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="#000000"
                android:textAlignment="center" />

            <!-- Description -->
            <TextView
                android:id="@+id/about_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingExtra="6dp"
                android:textSize="16sp"
                android:textColor="#333333"
                android:textAlignment="center"
                android:paddingTop="8dp"
                android:text="Cognisite is dedicated to early childhood cognitive and behavioral assessments. Our mission is to provide accessible tools that help parents, educators, and healthcare providers identify developmental patterns in children. We believe in using technology to empower better understanding and earlier interventions." />
        </LinearLayout>
    </ScrollView>
</FrameLayout>
